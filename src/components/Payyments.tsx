import React from "react";
import CardContainer from "./CardContainer";

type Props = {};

const Payyments = (props: Props) => {
  return (
    <div className="my-20">
      <CardContainer />
    </div>
  );
};

export default Payyments;
